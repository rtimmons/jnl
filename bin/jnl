#!/usr/bin/env bash
set -eou pipefail

root="$(dirname "$0")/.."
pushd "$root" >/dev/null
    root="$(pwd -P)"
popd >/dev/null

pushd "$root" >/dev/null
    "$root/venv/bin/python" "$root/bin/jnl.py" "$@"
popd >/dev/null
